import {GoldLogoComponent} from '@/components/gold-logo'; // Import the GoldLogo component from the components folder

export default function Home() {
  return (
    <main className="flex h-svh flex-col items-center justify-center">
      <GoldLogoComponent /> 
    </main>
  );
}
